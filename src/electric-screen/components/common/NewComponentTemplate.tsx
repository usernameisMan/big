import styled from "styled-components"

type Props = {}

export default function NewComponentTemplate({}: Props) {
  return (
    <Wrapper>ComponentTemplate</Wrapper>
  )
}

const Wrapper = styled.div`

`
