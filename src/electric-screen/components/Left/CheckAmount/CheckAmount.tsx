import styled from 'styled-components';
import SectionTitle from '../../common/SectionTitle';

type Props = {};

export default function CheckAmount({}: Props) {
  return (
    <div>
      <SectionTitle>检查总数</SectionTitle>
      <div></div>
    </div>
  );
}
